Hello! I have reviewed your code and made a small adjustment to ensure any laptop on the same Wi-Fi connection can connect smoothly.

Here are the details and what you need to do:

### 1. Code Changes Made
- I updated the frontend API service (`frontend/src/services/api.js`) to use relative paths (`/api`) instead of hardcoding `localhost`. This ensures that when a different device accesses `192.168.5.48`, the API calls are routed correctly through your Vite server to the backend.
- The WebRTC video call code (`useSignaling.js`) is already written correctly to adapt to whatever IP is in the browser URL (`window.location.host`). 
- STUN servers are correctly configured, so the Video Call services ARE runnable locally.
- Both your frontend (Vite) and backend (Spring Boot) servers are currently running properly.

### 2. How to Connect from the Other Laptop (Doctor/Patient)
To join from the other laptop, open a web browser (like Chrome or Edge) and go to:
👉 **https://192.168.5.48:5173**

### 3. IMPORTANT: Security Warning (SSL Certificate)
Because the app is running locally over HTTPS (which is REQUIRED to allow Camera and Microphone access for the video call), the browser on the other laptop will show a warning saying **"Your connection is not private"** or **"NET::ERR_CERT_AUTHORITY_INVALID"**.
- This is normal for local development!
- **What to do:** Click on the **"Advanced"** button at the bottom, and then click **"Proceed to 192.168.5.48 (unsafe)"**.
- If you are using Chrome and don't see the Proceed button, type `thisisunsafe` anywhere on the warning page, and it will bypass it.

### 4. IMPORTANT: Windows Firewall (If it times out)
If the other laptop tries to visit the link and it just keeps loading or says "Site cannot be reached", your Windows Firewall is blocking it.
- **What to do:** 
  1. Open the Start Menu, search for **Windows Defender Firewall** and open it.
  2. Click **Advanced settings** on the left.
  3. Click **Inbound Rules** -> **New Rule...** (on the right panel).
  4. Select **Port** -> Next -> **TCP** and specify local ports: **5173**.
  5. Select **Allow the connection** -> Next -> check all network types (Domain, Private, Public) -> Name it "Vite Frontend" and Finish.
  6. Repeat this process to create another rule for Port **8080** (Backend).

The app is ready for you to test the video call! Just ensure both laptops have a working camera/microphone and follow the security warning bypass above.
